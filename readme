

WEB SCRAPPING

TENER INSTALADO PYTHON 3 O SUPERIOR EN EL ORDENADOR

-Verifica que version tienes
python --version

-En la carpeta del proyecto que se clone del git
instala 
python -m pip install django
pip install django-cms

y luego selenium
pip install selenium


-Instala virtualenv en la carpeta del proyecto
en linea de comando ingresa a la carpeta del proyecto

pip install virtualenv

luego en la misma carpeta 

virtualenv venv

luego .\venv\Scripts\Activate

te deberia de mostrar

(venv) c:\User\TUPC\Desktop\djangoproject

-Ejecucion EN VISUAL
presiona F1 y escribe terminal
te saldra una ventana en la parte inferior de visual luego en una de esas pestañas esta una llamada
"TERMINAL" le das click luego a la derecha haces otra vez click en la flecha hacia abajo a la derecha del "+" y seleccionas command prompt
y ejecutas este comando "python manage.py runserver"


